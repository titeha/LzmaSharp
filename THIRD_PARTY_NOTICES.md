# Third-Party Notices / Провенанс компонентов

Документ фиксирует **происхождение каждого компонента** реализации `Lzma.Core` (из какого источника
он получен, под какой лицензией этот источник, каким способом выполнен перенос) и перечисляет
сторонние компоненты в сборке, тестах и эталонном `vendor/`.

Ключевой факт: **production-сборка `Lzma.Core` не содержит стороннего кода кодеков** — все алгоритмы
реализованы заново на управляемом C# (без нативных зависимостей, без `unsafe`). Единственная
сторонняя зависимость времени выполнения — стандартная библиотека .NET (BCL).

Сокращения способа переноса:
- **Порт (побайт.)** — перенос логики с эталонного исходника LZMA SDK; корректность подтверждена
  побайтовой сверкой с настоящими 7-Zip / `xz` на реальных архивах.
- **По спецификации** — самостоятельная реализация по опубликованному стандарту/RFC/описанию формата;
  корректность подтверждена официальными тест-векторами и/или round-trip-сверкой с эталоном.

---

## 1. Компоненты реализации `Lzma.Core` (собственный код)

| Компонент | Источник / основа | Лицензия источника | Способ переноса |
|---|---|---|---|
| LZMA (декодер + энкодер) | LZMA SDK (`LzmaDec.c` / `LzmaEnc.c`) | Public Domain | Порт (побайт.) |
| LZMA2 (декодер + энкодер) | LZMA SDK (`Lzma2Dec.c` / `Lzma2Enc.c`) | Public Domain | Порт (побайт.) |
| PPMd вар. H (Ppmd7, декодер + энкодер) | LZMA SDK (`Ppmd7.c` / `Ppmd7Dec.c` / `Ppmd7Enc.c`; PPMd var.H Д. Шкарина) | Public Domain | Порт (побайт.) |
| Фильтр BCJ2 (декодер + энкодер) | LZMA SDK (`Bcj2.c` / `Bcj2Dec.c` / `Bcj2Enc.c`) | Public Domain | Порт (побайт.) |
| Фильтры BCJ (x86/ARM/ARM64/ARMT/PPC/SPARC/IA64, декод) | LZMA SDK (`Bra.c` / `Bra86.c` / `BraIA64.c`) | Public Domain | Порт (побайт.) |
| Формат контейнера `.7z` (чтение и запись) | Спецификация формата 7z (эталон — LZMA SDK / 7-Zip) | Public Domain | Порт (побайт.) |
| BZip2 (декодер) | Формат BZip2 (спецификация формата) | — (общедоступный формат) | По спецификации |
| Deflate / Deflate64 (декодер) | RFC 1951 (Deflate); формат Deflate64 | — (открытый стандарт) | По спецификации |
| CRC-32 | Стандартный алгоритм (полином `0xEDB88320`) | — | По спецификации |
| 7zAES: схема контейнера (AES-256-CBC + SHA-256 KDF) | Формат 7zAES (7-Zip); примитивы AES/SHA — .NET BCL (см. §2) | Public Domain (формат) | По спецификации |
| ГОСТ: Кузнечик (блочный шифр) | ГОСТ Р 34.12-2015 / RFC 7801 | — (нац. стандарт / RFC) | По спецификации¹ |
| ГОСТ: Магма (блочный шифр) | ГОСТ Р 34.12-2015 / RFC 8891 | — | По спецификации¹ |
| ГОСТ: режим гаммирования (CTR) | ГОСТ Р 34.13-2015 | — | По спецификации¹ |
| ГОСТ: Стрибог (хеш 256/512) | ГОСТ Р 34.11-2012 / RFC 6986 | — | По спецификации¹ |
| ГОСТ: HMAC-Стрибог | RFC 7836 / RFC 2104 | — | По спецификации¹ |
| ГОСТ: KDF (KDF_GOSTR3411_2012_256) | RFC 7836 | — | По спецификации¹ |

¹ ГОСТ-ветка — **исследовательская, собственная managed-реализация**; корректность подтверждена
официальными тест-векторами соответствующих RFC/ГОСТ. Это **не** сертифицированное СКЗИ и не несёт
заявлений о защите информации (см. `docs/PROVENANCE.md`, редакция Community).

---

## 2. Сторонние компоненты времени выполнения (в сборке)

| Компонент | Назначение | Лицензия |
|---|---|---|
| .NET (BCL, `System.*`, включая `System.Security.Cryptography`: AES-256, SHA-256, RNG) | Базовая библиотека рантайма; криптопримитивы для 7zAES | MIT (Microsoft) |

Иных пакетных зависимостей у `Lzma.Core` нет (`<dependencies>` в NuGet-пакете пуст).

---

## 3. Эталонные исходники в `vendor/` (в сборку НЕ входят)

| Расположение | Назначение | Лицензия |
|---|---|---|
| `vendor/lzma-sdk/24.09`, `vendor/lzma-sdk/25.01` | Эталон для сверки портов (LZMA/LZMA2/PPMd/BCJ2/7z) | Public Domain |
| `vendor/GOST RFC` | Тексты RFC/ГОСТ для сверки ГОСТ-реализации | Тексты RFC/стандартов |

Точные версии и хеши эталонов — в `vendor/lzma-sdk/*/README` (SHA-256 для доказуемой целостности).

---

## 4. Зависимости только для тестов (не в production-сборке)

| Компонент | Назначение | Лицензия |
|---|---|---|
| SharpZipLib (NuGet) | Оракул round-trip-сверки собственного BZip2-декодера — **только в тест-проекте** | MIT |

`SharpCompress` (ранее — PPMd/Deflate64) полностью удалён после появления собственных декодеров.

---

*Данный документ — часть провенанс-пакета проекта; сопутствующее заявление о происхождении кода и
практиках разработки — в `docs/PROVENANCE.md` и `docs/SDL.md`.*
