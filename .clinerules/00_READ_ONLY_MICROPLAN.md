Прочитай активные правила проекта.

Это только read-only планирование. Ничего не изменяй.

Разбей задачу на микрошаги по принципу:

- один шаг = одна функция/класс/тестовый инвариант;
- новый файл: сначала каркас, затем по одной функции, затем тесты;
- не ломать зелёный код;
- максимум два файла на шаг;
- каждый шаг должен иметь одну команду проверки;
- каждый шаг должен быть отдельно отменяемым/коммитируемым.

Выдай:

1. Текущие факты из кода.
2. Полный список микрошагов.
3. Для каждого:
   - ID;
   - цель;
   - exact allowlist;
   - expected diff;
   - targeted build/test;
   - риск;
   - rollback.
4. Рекомендуемый первый микрошаг.

Не реализуй ничего.

Заверши:
PLAN_READY
или
BLOCKED
